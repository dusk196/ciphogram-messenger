import { DecryptMsgsPipe } from './decrypt-msgs.pipe';

describe('DecryptMsgsPipe', () => {
  it('create an instance', () => {
    const pipe = new DecryptMsgsPipe();
    expect(pipe).toBeTruthy();
  });
});
